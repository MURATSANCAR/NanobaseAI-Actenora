package com.nanobaseai.actenora.operations.domain.retention;

/** Resource kinds subject to retention deletion. */
public enum RetentionResourceType {
    TRANSCRIPT,
    PRIVATE_NOTE,
    OBJECT_STORAGE
}
