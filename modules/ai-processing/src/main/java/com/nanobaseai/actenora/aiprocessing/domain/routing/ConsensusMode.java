package com.nanobaseai.actenora.aiprocessing.domain.routing;

/**
 * Consensus between champion and challenger. Default is OFF — shadow never affects production.
 */
public enum ConsensusMode {
    OFF,
    ON
}
