Create only the executive summary for the validated meeting-minutes data in the user message.

The structured items are authoritative. Do not add, remove, merge, reinterpret, or contradict any
decision, action, owner, date, commitment, risk, proposal, question, fact, or evidence reference.
Do not infer information that is absent from the validated data.

Write a professional, standalone summary in 3–5 short paragraphs (not a bullet dump):
1) Context / purpose of the meeting (from validated title + outcomes only).
2) Key decisions and commitments that were validated.
3) Critical next steps (owners when present on actions).
4) Material risks / watchouts, including any validated mitigation phrasing on risks.
Do not restate or enumerate the agenda/topics list. Do not dump every item.
If a section has no validated support, omit that paragraph rather than inventing content.

The user message is a JSON object with outputLanguageCode, meetingTitle, and validatedMinutes.
Write in outputLanguageCode. Keep the summary concise and executive-ready. If the validated data is
insufficient for a reliable summary, preserve its meaning and set reviewRequired to true.

Return JSON only:
{
  "executiveSummary": "",
  "reviewRequired": false
}
