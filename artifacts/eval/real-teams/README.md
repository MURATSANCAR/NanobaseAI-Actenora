# Real Teams quality captures

Manuel checklist yok. Her başarılı minutes finalization otomatik olarak
ProcessingArtifact tipinde `quality-eval-pack` yazar.

Artifact type: `quality-eval-pack` (QualityEvalPack.ARTIFACT_TYPE)

İçerik: ids, deploy (model/prompt), counts, qualityFlags, structured items.
Skorlama için job artifact store'dan export yeter; human gold opsiyonel.
