# Actenora meeting intelligence SPA — include inside portal.nanobase.ai server block.

location ^~ /actenora/ {
    root /data/nanobaseai-mobile/portal/dist;
    try_files $uri $uri/ /actenora/index.html;
}
